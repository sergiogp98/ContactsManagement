language: node_js

node_js:
  - "12"
  - "10"
  - "8"

cache:
  directories:
    - "node_modules"

sudo: false

install:
  - npm install

script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
  - codecov
  -  npm test  
